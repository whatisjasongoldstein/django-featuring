============
Installation
============

At the command line::

    $ easy_install django-featuring

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-featuring
    $ pip install django-featuring